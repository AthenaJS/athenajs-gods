import RM from 'ResourceManager';
import MapEvent from 'MapEvent';
import Sprite from 'Sprite';

class GodsMapEvent extends MapEvent {
    constructor(map) {
        super(map);
    }
};

export default GodsMapEvent;